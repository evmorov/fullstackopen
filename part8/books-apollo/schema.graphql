# eslint-disable @graphql-eslint/executable-definitions

type Book {
  id: ID!
  title: String!
  author: String!
  published: Int!
  genres: [String]!
}

type Query {
  bookCount: Int!
  authorCount: Int!
  allBooks: [Book!]!
}
